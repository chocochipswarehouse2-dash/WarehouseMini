const fs = require('fs');
// Let's create a small script that fetches the master_produk from Supabase using REST API
// Wait, better yet, we can check the localStorage if we run it in browser, but we can't.
// Let's check where the data was seeded from.
